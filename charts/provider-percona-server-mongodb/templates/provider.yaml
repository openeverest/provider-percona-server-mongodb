apiVersion: core.openeverest.io/v1alpha1
kind: Provider
metadata:
  name: {{ .Values.provider.name }}
  labels:
    {{- include "provider-percona-server-mongodb.labels" . | nindent 4 }}
# Spec is generated by `provider-sdk generate` from definition/ files.
# Run `make generate` to update.
# Uses `tpl` to resolve Helm values referenced in uiSchema defaultValues.
{{ tpl (.Files.Get "generated/provider-spec.yaml") . | nindent 0 }}
