# UI rendering hints for this secret type.
label: User Credentials Secret
componentsOrder:
  - databaseAdminUser
  - databaseAdminPassword
  - clusterAdminUser
  - clusterAdminPassword
  - backupUser
  - backupPassword
  - clusterMonitorUser
  - clusterMonitorPassword
  - userAdminUser
  - userAdminPassword
components:
  backupUser:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_BACKUP_USER" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Backup User"
    validation:
      required: true
  backupPassword:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_BACKUP_PASSWORD" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Backup Password"
    validation:
      required: true
  clusterAdminUser:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_CLUSTER_ADMIN_USER" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Cluster Admin User"
    validation:
      required: true
  clusterAdminPassword:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_CLUSTER_ADMIN_PASSWORD" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Cluster Admin Password"
    validation:
      required: true
  clusterMonitorUser:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_CLUSTER_MONITOR_USER" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Cluster Monitor User"
    validation:
      required: true
  clusterMonitorPassword:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_CLUSTER_MONITOR_PASSWORD" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Cluster Monitor Password"
    validation:
      required: true
  databaseAdminUser:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_DATABASE_ADMIN_USER" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Database Admin User"
    validation:
      required: true
  databaseAdminPassword:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_DATABASE_ADMIN_PASSWORD" # TODO: use data for base64 encoded text
    fieldParams:
      label: "Database Admin Password"
    validation:
      required: true
  userAdminUser:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_USER_ADMIN_USER" # TODO: use data for base64 encoded text
    fieldParams:
      label: "User Admin User"
    validation:
      required: true
  userAdminPassword:
    uiType: text # TODO: use base64 encoded text
    path: "stringData.MONGODB_USER_ADMIN_PASSWORD" # TODO: use data for base64 encoded text
    fieldParams:
      label: "User Admin Password"
    validation:
      required: true
