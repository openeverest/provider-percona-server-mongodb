apiVersion: backup.openeverest.io/v1alpha1
kind: Backup
metadata:
  name: psmdb-src-backup
spec:
  origin:
    type: Instance
    instanceRef:
      name: psmdb-src
  classRef:
    name: percona-backup-mongodb
  storageRef:
    name: minio-storage
